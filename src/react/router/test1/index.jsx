import React, { PureComponent, Component } from 'react';

export default class extends (PureComponent || Component) {
    render() {
        return (
            <div>
                1
            </div>
        )
    }
}